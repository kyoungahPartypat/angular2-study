import { Component } from '@angular/core';


@Component({
  selector: 'video-input',
  styles: [`

    `],
  template: `
    <div>
      <select></select>
      <input type = "text" />
      <button>확인</button>
    </div>
  `
})

export class VideoInputComponent {}
